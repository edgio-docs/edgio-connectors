export const withXDN = require('../withXDN') // we use require here for backwards compatibility since withXDN has always used `export =` instead of `export default`
export { default as withServiceWorker } from '../sw/withServiceWorker'
