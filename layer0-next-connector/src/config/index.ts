export const withLayer0 = require('../withLayer0') // we use require here for backwards compatibility since withLayer0 has always used `export =` instead of `export default`
export { default as withServiceWorker } from '../sw/withServiceWorker'
